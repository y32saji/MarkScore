export interface IMermaidMusicPlugin {
  register: () => void
}

const plugin: IMermaidMusicPlugin = {
  register(): void {
    // Plugin registration logic will be implemented here
  }
}

export default plugin